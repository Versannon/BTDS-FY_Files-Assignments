#include<stdio.h>
int main()
{
    int x,y;
    x=10;
    y=20;
    if (x!=y)
    {
        printf("The Given Numbers : x = %d and y= %d are not equal to each other",x,y);
    }
    else{
        printf("They are equal");
    }
    return 0;
}