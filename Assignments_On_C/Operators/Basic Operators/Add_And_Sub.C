#include<stdio.h>
int main()
{
    /*Here I have declared 4 variables where 3 of them will hold values and a will be the variable on which the operations will be performed*/
    int x,y,z,a;
    x=100;
    y=40;
    z=60;
    a=x-(y+z);
    printf("%d",a);
    return 0;
}