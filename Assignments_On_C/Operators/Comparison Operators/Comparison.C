#include<stdio.h>
int main()
{
    int x,y,z,a,b,c;
    x=15;
    y=17;
    z=20;
    a = x; // Assign x to a
    if(a<=y)
    {
        printf("A is Smaller %d",a); // Correct formatting
        printf("\n");
        return 0;
    }
}
