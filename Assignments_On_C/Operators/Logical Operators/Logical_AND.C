#include <stdio.h>

int main() {
    int x, y;
    x=10;
    y=30;
    if (x > 0 && y > 0) {
        printf("Both numbers are greater than 0.\n");
    } else {
        printf("Neither one number is not greater than 0.\n");
    }

    return 0;
}